# Java_CSV_ValidationEngine
A Java-based CSV normalization and validation engine that enforces schema-level rules, ensures data quality, and generates clean datasets along with rejection reports and audit summaries.
